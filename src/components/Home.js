import React, {useEffect, useRef} from 'react';

const Home = (props) => {

    useEffect(() => {
        //focus input
        console.log('use effect hook ran');
    },[]);

    return (
        <div>
            {/*<h3>Items</h3>*/}
        </div>
    );
};

export default Home